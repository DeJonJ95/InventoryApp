# Step 001: Updated QR tag PDF to exact Avery 5160 dimensions

## Context
Updated `lib/printTags.js` to use exact Avery 5160 address-label measurements from the template at `Downloads\Avery5160AddressLabels.doc`. The previous code used calculated sizes (dividing page space evenly) which didn't match the physical label boundaries, causing misalignment when printing on actual label sheets.

## Result
**pass** — `next build` compiles without errors.

Changes:
- `MARGIN_X`: 10 → 4.7625 mm (Avery spec 0.1875″)
- `MARGIN_Y`: 12 → 12.7 mm (Avery spec 0.5″)
- Label grid now uses fixed pitch (69.85 mm × 25.4 mm) instead of dividing available space
- Removed dashed cut-guide rectangles (labels have physical boundaries)
- QR size: 16 → 15 mm for more room underneath on the 1″-tall label
- Item name: truncated to 38 chars at word boundary instead of multi-line wrap (avoids overflow)
- Item ID: 8pt → 7.5pt, Item name: 7pt → 6.5pt for tighter fit

## Next
None — this task is complete. Next session can pick up whatever's next.